/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.dribbble.com",
      "red-personal-mockingbird-716.mypinata.cloud",
    ],
  },
};

export default nextConfig;
